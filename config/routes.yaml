root.get.v1:
    path: /
    controller: resource.root:get
    methods: [GET]
    options:
        stability: stable
        deprecated: []
        version: 1

domains.post.v1:
    path: /domains
    controller: resource.domains:post
    methods: [POST]

domains.domain.delete.v1:    
    path: /domains/{domain}
    controller: resource.domains.domain:delete
    methods: [DELETE]
    options:
        stability: stable
        deprecated: []
        version: 1

domains.domain.disable.post.v1:
    path: /domains/{domain}/actions/disable
    controller: resource.domains.domain.disable:post
    methods: [POST]
    options:
          stability: stable
          deprecated: []
          version: 1

domains.domain.enable.post.v1:
    path: /domains/{domain}/actions/enable
    controller: resource.domains.domain.enable:post
    methods: [POST]
    options:
          stability: stable
          deprecated: []
          version: 1

domains.domain.get.v1:
    path: /domains/{domain}
    controller: resource.domains.domain:get
    methods: [GET]
    options:
        stability: stable
        deprecated: []
        version: 1
